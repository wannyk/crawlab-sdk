const utils = require('./utils')

module.exports = {
  saveItem: utils.saveItem,
  find: utils.find,
  update: utils.update,
  delete: utils.delete,
  findLatest: utils.findLatest,
  close: utils.close
}
